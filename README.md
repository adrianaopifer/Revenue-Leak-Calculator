# Revenue Leak Calculator

Production site: <https://calculator.opiferai.com/>

The live page is `index.html`. `OPIFER_Revenue_Leak_Calculator.html` is a legacy file and is not deployed.

Before publishing, follow `PUBLISHING.md` and run:

```bash
python3 scripts/validate-seo.py
```
